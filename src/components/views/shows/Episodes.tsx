import React from "react";
import { ISeason } from "../../../redux/firebase/interfaces";


export const Episodes = ({item} : {item: ISeason}) => {
 
return (
  <div className="my-5">
    
  </div>
)
}